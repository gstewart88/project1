<h1>Listing Games</h1>
<p><%= User.count %> players have played a total of <%= Game.count %> games</p>
<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Turn</th>
    </tr>
  </thead>

  <tbody>
    <% @games.each do |game| %>
      <tr>
        <td><%= game.id %></td>
        <td><%= game.whose_turn.name %></td>
        <td><%= link_to 'Show', game %></td>
      </tr>
    <% end %>
  </tbody>
</table>
</br>
<%= link_to 'New Game', new_game_path %>
</br>