<%= form_for(@game) do |f| %>
<div class="field">
  <%= f.label :character_id %><br>
  <%= f.select :character_id, Character.all.map {|c| [c.name, c.id] } %>
</div>
<div class="field">
  <%= f.label :character_id %><br>
  <%= f.select :character_id, Character.all.map {|c| [c.name, c.id] } %>
</div>
<%= f.button %>
<% end %>